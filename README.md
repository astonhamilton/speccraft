# spacecraft
SpecCraft is an AI tool experiment that turns early architecture discussions into structured, compliant specs. It keeps a persistent model that evolves over multiple turns, avoiding the pitfalls of one-shot LLMs or heavy multi-agent setups
